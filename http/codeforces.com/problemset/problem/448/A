#include<bits/stdc++.h>
using namespace std;

int main()
{
    // Kak Kak Kak
    int x , a=0 ,b=0 , n ;
    for(int i=0 ; i<3 ; i++)
    {
        cin>>x;
        a+=x;
    }
    for(int i=0 ; i<3 ; i++)
    {
        cin>>x;
        b+=x;
    }
    cin>>n;
    n-=(a/5);
    n-=(b/10);
    a=a%5;
    b=b%10;
    if(a>0)
        n--;
    if(b>0)
        n--;
    if(n>=0)
        cout<<"YES"<<endl;
    else
        cout<<"NO"<<endl;
	return 0 ;
}
